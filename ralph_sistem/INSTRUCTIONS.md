# Istruzioni Sistema Ralph: compliance-check

## Requisiti di Sistema
- Git
- jq (Processore JSON da riga di comando)
- Python 3.8+ (per TUI/Orchestrator)
- Node.js (se si usano progetti JS/TS)
- Tool CLI selezionato installato (vedi sotto)

## Installazione & Setup

### Google Project IDX / Antigravity
1. Apri il progetto in Google IDX o Cloud Shell.
2. Il file `.idx/dev.nix` installerà automaticamente `jq`, `gcloud` e `python3`.
3. Attendi la ricostruzione dell'ambiente.

Gestito via `gcloud auth login`.

## Esecuzione di Ralph

1. Rendi gli script eseguibili:
   ```bash
   chmod +x run_ralph.sh
   
   ```

2. Inizializza Git (per cronologia atomica):
   ```bash
   git init
   git add .
   git commit -m "Initial Ralph Setup"
   ```

3. Avvia il Ciclo:
   ```bash
   ./run_ralph.sh
   ```

## Ciclo di Lavoro
1. Ralph legge `prd.json` per trovare il prossimo compito incompleto.
2. Legge `agents.md` per il contesto a lungo termine.
3. Genera un prompt e chiama **gemini-3-pro-preview**.
4. Tu verifichi l'output. Se valido, il compito è segnato come fatto.
5. Ripeti finché tutti i compiti sono completi.

---
*Generated by Ralph System Generator (2026 Edition)*
