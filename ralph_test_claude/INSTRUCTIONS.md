# Ralph System Instructions: Test_ralph_test_claude

## System Requirements
- Git
- jq (Command-line JSON processor)
- Python 3.8+ (for TUI/Orchestrator)
- Node.js (if using JS/TS projects)
- Selected CLI Tool installed (see below)

## Installation & Setup

### Claude Code CLI (Official)
1. Install Claude Code CLI:
   `npm install -g @anthropic-ai/claude-code`
2. Authenticate (opens browser):
   `claude`

### API Keys Configuration
Authentication is handled via browser login when you first run `claude`.
No API key needed if you have Claude Pro/Max/Teams subscription.

## Running Ralph

1. Make scripts executable:
   ```bash
   chmod +x run_ralph.sh
   
   ```

2. Initialize Git (for atomic history):
   ```bash
   git init
   git add .
   git commit -m "Initial Ralph Setup"
   ```

3. Start the Loop:
   ```bash
   ./run_ralph.sh
   ```

## Workflow Loop
1. Ralph reads `prd.json` to find the next incomplete task.
2. It reads `agents.md` for long-term context.
3. It generates a prompt and calls **claude-3-5-sonnet-latest**.
4. You verify the output. If valid, the task is marked done.
5. Repeat until all tasks are complete.

---
*Generated by Ralph System Generator (2026 Edition)*
